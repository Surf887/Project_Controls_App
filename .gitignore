node_modules/
dist/
server/dist/
# Runtime data + secrets — never commit. Keep the directory via .gitkeep.
server/data/*
!server/data/.gitkeep
test-results/
*.log
.env
.env.local
# Local Claude Code harness state (worktrees, local settings) — never commit.
.claude/
# Design review source archive; contents live in design-handoff/.
App design review.zip
